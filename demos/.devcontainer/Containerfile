FROM ubuntu:26.04

RUN apt update && \
    apt -y install \
        build-essential \
        gdb \
        git \
        libcairo2-dev \
        libegl-dev \
        libgles-dev \
        libwayland-dev \
        mangohud \
        meson \
        pkg-config \
        valgrind \
        wayland-protocols && \
    apt -y clean && \
    rm -rf /var/lib/apt/lists/*

RUN userdel -f -r ubuntu

